-= In the future =-
(In no particular order)

OpenBSD support.
Encapsulation (pod/vidcast) support.
Separation into client server.
  Command line (non ncurses) interface to access.
Support for methods other than HTTP/FTP (running scripts);
Eliminate external HTTP client (wget/curl) requirement.
